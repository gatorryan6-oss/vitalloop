__pycache__/
*.pyc
.venv/
venv/
.pytest_cache/
.pytest_tmp/
instance/

# Runtime data, never source: the attempts log is a file of team scores
# written on the teacher's machine (Phase 8 kickoff SS2).
data/
Thumbs.db
.DS_Store

# Claude Code local noise — but settings.json IS project config (kickoff M0)
.claude/*
!.claude/settings.json
